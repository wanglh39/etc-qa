# ETC客服QA智能检索系统 — 运行时依赖清单
# Python >= 3.10
# 安装: uv pip install -r requirements.txt

# Web框架
fastapi>=0.104.0
uvicorn>=0.24.0
python-dotenv>=1.0.0
python-multipart>=0.0.6
filelock>=3.13.0

# 阿里云NLS（实时语音识别ASR）
# aliyunsdkcore需单独安装: uv pip install aliyunsdkcore --no-deps（避免pycrypto编译失败）
aliyunsdkcore>=1.0.3  # 安装时加 --no-deps
pycryptodome>=3.20.0  # pycrypto的纯Python替代（aliyunsdkcore依赖pycrypto，用pycryptodome替代）
websocket-client>=1.8.0

# 定时任务调度
apscheduler>=3.10.0

# 认证
pyjwt>=2.8.0

# 数据模型
pydantic>=2.0.0

# 数据库
pymysql>=1.1.0
pymilvus==3.0.0
milvus-lite>=3.0.0

# BM25
jieba>=0.42.1
rank-bm25>=0.2.2

# 数值计算（rag/siliconflow.py 顶层 import numpy）
numpy>=1.24.0

# HTTP客户端（SiliconFlow 嵌入/重排 API）
httpx>=0.25.0

# Agent (LangGraph + LLM)
langgraph>=0.0.30
langchain-openai>=0.0.5
langsmith>=0.1.0
jinja2>=3.1.0
